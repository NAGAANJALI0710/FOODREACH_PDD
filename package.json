{
    "name": "foodshare-root",
    "version": "1.0.0",
    "scripts": {
          "install": "cd backend && npm install --production=false && npm run build",
          "start": "cd backend && npm start"
    }
}
